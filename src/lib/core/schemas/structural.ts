// Structural primitive block schemas — filled by per-primitive tasks (10–16).
// Kept as a single file so the circular import pattern (Block ↔ structural
// primitives) resolves cleanly via ESM live-bindings + z.lazy deferral.
export {};
